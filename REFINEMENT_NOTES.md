# Nata Website – Layout Refinement

This version keeps the same content and functionality but fixes the visual density:

- much larger vertical spacing between sections
- wider content canvas
- calmer hero composition
- larger editorial typography
- more breathing room in cards
- clearer feature storytelling
- larger image stages
- more separated CTA and FAQ sections
- better mobile spacing
- subtle scroll progress indicator
- section separators for orientation

The standalone `index.html` still works directly and loads `styles.css` + `script.js`.
